<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.warehouse.dao.ShoppingDao">
    <select id="countFilteredCommodities" resultType="long">
        SELECT COUNT(*) FROM commodity
        <where>
            <if test="productType != null">
                AND product_type = #{productType}
            </if>
            <if test="productName != null">
                AND product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
            <if test="minPrice != null and maxPrice != null">
                AND original_price BETWEEN #{minPrice} AND #{maxPrice}
            </if>
        </where>
    </select>
    <select id="selectCommodities" resultType="com.warehouse.pojo.Commodity">
        SELECT * FROM commodity
        <where>
            <if test="productType != null">
                AND product_type = #{productType}
            </if>
            <if test="productName != null">
                AND product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
            <if test="minPrice != null and maxPrice != null">
                AND original_price BETWEEN #{minPrice} AND #{maxPrice}
            </if>
        </where>
        <choose>
            <when test="sortBy == 'type'">
                ORDER BY product_type ${order}
            </when>
            <when test="sortBy == 'discountedPrice'">
                ORDER BY original_price * commodity_discount ${order}
            </when>
            <otherwise>
                ORDER BY commodity_id ${order}
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>